// Generated by CoffeeScript 1.10.0
(function() {
  var client, job, mqtt, options, r, sendMsg;

  mqtt = require("mqtt");

  options = {
    port: 6260
  };

  client = mqtt.connect('ws://192.168.16.175', options);

  r = 0;

  job = null;

  sendMsg = function(message) {
    var obj;
    r = Math.floor((Math.random() * 10) + 1);
    obj = {
      name: "brian",
      num: r
    };
    return client.publish("brian/", JSON.stringify(obj));
  };

  client.on('connect', function() {
    console.log("Connected !!");
    return job = setInterval(sendMsg, 2000);
  });

}).call(this);
